[project]
name = "pakedge_api"
version = "0.1.0"
description = "Python API client for Pakedge managed switches"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31",
]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
